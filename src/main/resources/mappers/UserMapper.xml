<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.backend.dao.UserDAO">

    <resultMap id="userMapper" type="User">
        <id column="user_id" property="userId"></id>
        <result column="username" property="userName"></result>
        <result column="password" property="userPwd"></result>
        <result column="nickname" property="nickName"></result>
        <result column="realname" property="realName"></result>
        <result column="user_img" property="userImg"></result>
        <result column="user_mobile" property="userMobile"></result>
        <result column="user_email" property="userEmail"></result>
        <result column="user_sex" property="userSex"></result>
        <result column="user_birth" property="userBirth"></result>
        <result column="user_regtime" property="userRegtime"></result>
        <result column="user_modtime" property="userModtime"></result>
    </resultMap>

    <insert id="insertUser" useGeneratedKeys="true" keyProperty="userId">
        insert into users
        values(null,#{userName},#{userPwd},#{nickName},#{realName},#{userImg},#{userMobile},#{userEmail},#{userSex},#{userBirth},#{userRegtime},#{userModtime})
    </insert>

    <select id="queryByUserName" resultMap="userMapper">
        select user_id,username,password,nickname,realname,user_img,user_mobile,user_email,user_sex,user_birth,user_regtime,user_modtime
        from users where username = #{userName}
    </select>

    <select id="queryByUserNameAndPwd" resultMap="userMapper">
        select user_id,username,password,nickname,realname,user_img,user_mobile,user_email,user_sex,user_birth,user_regtime,user_modtime
        from users where username = #{userName} and password = #{userPwd}
    </select>

</mapper>